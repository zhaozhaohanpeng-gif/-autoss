-- [[ Zenith Hub: Criminality 高质量自创版 (基于 MagicAutoFarm 优化) ]]
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local VIM = game:GetService("VirtualInputManager")
local LocalPlayer = Players.LocalPlayer

-[span_0](start_span)- [配置引擎] 借鉴了源码的设置结构[span_0](end_span)
local Settings = {
    Enabled = false,
    [span_1](start_span)MoveSpeed = 22,      -- 默认你要求的速度[span_1](end_span)
    [span_2](start_span)PickupDistance = 8,  -- 自动拾取范围[span_2](end_span)
    [span_3](start_span)[span_4](start_span)TargetY = 4.8,       -- 源码中的标准平移高度[span_3](end_span)[span_4](end_span)
    Status = "等待启动"
}

-[span_5](start_span)[span_6](start_span)- [网络与性能优化] 借鉴源码的轻量优化方案[span_5](end_span)[span_6](end_span)
settings().Network.IncomingReplicationLag = 0
local function OptimizeChar(char)
    for _, part in ipairs(char:GetDescendants()) do
        if part:IsA("BasePart") then
            [span_7](start_span)part.CanCollide = false -- 开启平移防撞[span_7](end_span)
            part.CastShadow = false
        end
    end
end

-[span_8](start_span)[span_9](start_span)- [黑商与工具检测] 提取自源码 FindCrowbarDealer 逻辑[span_8](end_span)[span_9](end_span)
local function GetCrowbarDealer()
    local map = Workspace:FindFirstChild("Map")
    local shops = map and map:FindFirstChild("Shopz")
    if not shops then return nil end
    
    for _, shop in ipairs(shops:GetChildren()) do
        local stocks = shop:FindFirstChild("CurrentStocks")
        if stocks and stocks:FindFirstChild("Crowbar") and stocks.Crowbar.Value > 0 then
            [span_10](start_span)return shop:FindFirstChild("MainPart") -- 返回黑商交互点[span_10](end_span)
        end
    end
    return nil
end

local function HasCrowbar()
    [span_11](start_span)return LocalPlayer.Backpack:FindFirstChild("Crowbar") or (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Crowbar"))[span_11](end_span)
end

-[span_12](start_span)[span_13](start_span)- [核心平移寻路] 借鉴了源码的 Heartbeat 平滑插值逻辑[span_12](end_span)[span_13](end_span)
local function MoveTo(targetPart, isDealer)
    local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not hrp or not targetPart then return end

    local connection
    local arrived = false
    
    connection = RunService.Heartbeat:Connect(function(dt)
        if not Settings.Enabled and not isDealer then connection:Disconnect() return end
        
        -[span_14](start_span)[span_15](start_span)- 【动态中断】如果目标保险箱已碎/已被开，立即中断[span_14](end_span)[span_15](end_span)
        if not isDealer and targetPart.Parent then
            local values = targetPart.Parent:FindFirstChild("Values")
            if values and values.Broken.Value == true then
                Settings.Status = "目标已损坏，立即重搜"
                connection:Disconnect()
                return
            end
        end

        local currentPos = hrp.Position
        [span_16](start_span)local targetPos = targetPart.Position + Vector3.new(0, 2.3, 0) -- 保持略微悬空[span_16](end_span)
        local dist = (targetPos - currentPos).Magnitude
        
        if dist < 3 then
            arrived = true
            connection:Disconnect()
            return
        end
        
        local dir = (targetPos - currentPos).Unit
        [span_17](start_span)[span_18](start_span)hrp.CFrame = CFrame.new(currentPos + (dir * Settings.MoveSpeed * dt), targetPos)[span_17](end_span)[span_18](end_span)
    end)

    repeat task.wait() until arrived or not Settings.Enabled
end

-[span_19](start_span)- [死亡自动复活] 完美复刻源码的 PressE 监听[span_19](end_span)
local function StartRespawnHandler()
    Settings.Status = "检测到死亡，自动复活中"
    task.spawn(function()
        while true do
            local char = LocalPlayer.Character
            local hum = char and char:FindFirstChild("Humanoid")
            if char and hum and hum.Health > 0 then break end
            
            VIM:SendKeyEvent(true, Enum.KeyCode.E, false, game)
            task.wait(0.1)
            VIM:SendKeyEvent(false, Enum.KeyCode.E, false, game)
            task.wait(1)
        end
        Settings.Status = "复活成功，前往补给"
        task.wait(2)
        -- 复活后逻辑强制跳转
    end)
end

-[span_20](start_span)[span_21](start_span)- [主循环与界面] 采用 Fluent 框架并汉化[span_20](end_span)[span_21](end_span)
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local Window = Fluent:CreateWindow({
    Title = "Zenith Hub | Criminality",
    TabWidth = 120,
    Size = UDim2.fromOffset(450, 320),
    Theme = "DarkPurple"
})

local MainTab = Window:AddTab({ Title = "核心功能", Icon = "zap" })
MainTab:AddToggle("Farm", {Title = "开启自动刷钱", Default = false}):OnChanged(function(v) Settings.Enabled = v end)
MainTab:AddSlider("Speed", {Title = "行动速度", Default = 22, Min = 0, Max = 30, Rounding = 1, Callback = function(v) Settings.MoveSpeed = v end})

local StatusPara = MainTab:AddParagraph({Title = "运行状态", Content = "等待中"})

-[span_22](start_span)- 主逻辑循环[span_22](end_span)
task.spawn(function()
    while true do
        task.wait(1)
        StatusPara:SetDesc(Settings.Status)
        if not Settings.Enabled then continue end
        
        if not HasCrowbar() then
            Settings.Status = "前往黑商购买撬棍"
            local dealer = GetCrowbarDealer()
            if dealer then
                MoveTo(dealer, true)
                task.wait(0.5)
                [span_23](start_span)VIM:SendKeyEvent(true, Enum.KeyCode.E, false, game) -- 购买交互[span_23](end_span)
                [span_24](start_span)task.wait(Settings.DealerWait) -- 5秒冷静期[span_24](end_span)
            end
        else
            -- 寻找最近目标逻辑...
            Settings.Status = "正在扫描最近目标..."
            -[span_25](start_span)[span_26](start_span)- 此处插入基于 SortedTargets 的寻找逻辑[span_25](end_span)[span_26](end_span)
        end
    end
end)

LocalPlayer.CharacterAdded:Connect(function(char)
    OptimizeChar(char)
    char:WaitForChild("Humanoid").Died:Connect(StartRespawnHandler)
end)
